/// @description Inserte aquí la descripción
// Puede escribir su código en este editor

var _delta = delta_time / MILLION
var _length = _speed * 100 * _delta

x += lengthdir_x(_length, rotation)
y += lengthdir_y(_length, rotation)